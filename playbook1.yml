---
- hosts: web1
  become: true
  become_user: ubuntu
  

  tasks:
  - name: Install all required dependencies
    apt:
        name : "{{ item }}"
        state: present
    loop:
        - python3
        - python-setuptools
        - python-dev
        - build-essential
        - python-pip 
        - python-mysqldb